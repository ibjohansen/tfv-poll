[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  SECRETS_SCAN_OMIT_KEYS = "MATRIKKEL_SYNC_EMAILS,API_MATRIKKEL_BASE_URL,NEON_STORAGE_REGION,AUTH_URL"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
